<?php

it('documents cursor ordering contract: kind then lowercase name then id', function () {
    $this->markTestSkipped('Scaffold: add deterministic ordering assertions for cursor pagination.');
});

it('documents cursor encoding/decoding invariants', function () {
    $this->markTestSkipped('Scaffold: roundtrip valid cursor payloads and invalid payload rejection.');
});
